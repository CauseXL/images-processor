// * --------------------------------------------------------------------------- store

import { mockPageData } from "@/mock";
import { store } from "../utils";

export const pageData = store(mockPageData);
